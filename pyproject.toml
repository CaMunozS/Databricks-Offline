[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "offline-model-transport"
version = "0.1.0"
description = "Staging and offline validation utilities for transportable ML models"
requires-python = ">=3.11,<3.12"
